# parkora-backoffice/.env
# Adresse IP de ton serveur FastAPI sur le réseau local.
# Même valeur que BACKEND_IP dans parkora-app/src/constants/config.ts
# Lance `ipconfig` (Windows) ou `ifconfig` (Linux/Mac) pour la trouver.

VITE_BACKEND_IP=192.168.1.38
